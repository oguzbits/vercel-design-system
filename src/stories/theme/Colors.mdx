import { Meta, Canvas } from "@storybook/blocks";
import * as ThemeStories from "./Theme.stories";

<Meta title="Theme/Colors" />

# Colors

Our color system is built on localized design tokens that ensure semantic consistency and accessibility across different themes and contexts.

<Canvas of={ThemeStories.Colors} sourceState="none" />
